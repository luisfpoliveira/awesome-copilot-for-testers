---
description: 'Instructions for <scope>. Use when working on <files-or-folder>.'
applyTo: '<glob-pattern>'
title: '<Human friendly title>'
name: '<kebab-case-name>'
---

## Scope

- Applies to: `<file pattern or folder>`
- Goal: `<what behavior this instruction should reinforce>`

## Rules

- **Prefer** `<preferred approach>` because <reason>.
- **Avoid** `<anti-pattern>` because <consequence>.
- Keep `<artifact type>` in `<location>`.
- Name `<artifact>` as `<convention>`.

### Preferred pattern

```ts
// Replace with a project-specific example that demonstrates the preferred pattern.
```

### Avoid

```ts
// Replace with a project-specific example that demonstrates the anti-pattern.
```

## Notes

- Scripts Copilot should prefer: `<npm run ...>`
- Files to inspect first: `<path>`, `<path>`
- Remove this section if it adds no value.
